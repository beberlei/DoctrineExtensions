language: php

php:
  - 7.2
  - 7.3
  - 7.4

install:
  - composer self-update
  - composer install

notifications:
  email: false

script:
  - make test
  - if [[ $TRAVIS_PHP_VERSION == 7.4 ]]; then make lint; fi

dist: bionic

cache:
  directories:
    - $COMPOSER_CACHE_DIR
